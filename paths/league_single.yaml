get:
  operationId: getLeagueByCode
  parameters:
    - name: code
      in: path
      description: Get a league by code
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/League'
      description: Get a league by code
